<h1><%= pluralize(@wines.size, 'wine') %></h1>
<%= page_entries_info @wines %>

<ol>
	<% @wines.each do |wine| %>
	<li><%= link_to(wine.name, wine) %></li>
	<% end %>
</ol>

<%= paginate @wines %>


<!-- <%= link_to 'New Wine', new_wine_path %> -->